import { Button } from "./shared/ui/Button/Button";

const App = () => {
    return (
        <div>
            <Button variant="secondary">
                Button
            </Button>
            <Button>
                Button
            </Button>
        </div>
    );
};

export default App;
